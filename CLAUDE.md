# AI 协作行为规则

> 本文件只管**怎么做**（沟通、编码、文件管理）。项目内容（产品定义、架构、品牌保护、进度规划）全部在 **`PROJECT.md`**。

---

## 协作前确认

下列动作执行前必须先和用户确认：

- 删除/重命名 `agents/` 下任何文件
- 修改品牌保护相关内容（SOUL 敏感问题应对、禁令清单、sanitize）
- 改 FastClaw Fork 源码（`fastclaw/`，vendored）
- 删除 `backend/`（废弃但未清理）
- 重启 FastClaw daemon（会断朋友会话）

下列动作可以直接做：

- 在 `agents/` 下新增 SKILL 草稿
- 在 `docs/` 下加设计文档
- 跑只读命令（git/ls/grep 等）

## 沟通

- 用中文沟通
- 回答要明确，不用"可能""也许""应该""大概"等模糊措辞

## 编码行为

### 动手前

- 明确说出假设。有多种理解时，列出来让用户选
- 发现更简单的方案时，主动说出来
- 有不清楚的地方，停下来问

### 执行中

- 只写解决问题所需的最少代码
- 只改必须改的行，保持原有风格

### 完成后

- 对照原始需求检查
- 有遗漏时主动说明

## 文件管理

### 项目结构

```
~/projects/
├── amazon/       # 亚马逊运营
├── content/      # 内容素材库
├── dev/          # 全栈开发学习
└── reading/      # 读书笔记
```

### 任务产出 → `tasks/` 目录

- 目录命名：`YYYYMMDD-{简短主题}`
- 每个任务目录必须有 `README.md`
- 日常问答、临时查询不需要建 tasks

### 统一规则

1. 内部知识用 `.md` 管理
2. 外部文件（.doc/.pdf/.xlsx）→ 原始文件存 `specs/` 或 `data/`，核心数据提取到 `.md`
3. 中文描述性命名，日期前缀 `YYYYMMDD`
4. 任务状态：进行中 / 已完成 / 待跟进

## 工作日志

- **文件**：`WORK_LOG.md`（唯一日志文件，不在其他地方记）
- **AI 必须主动记录**。每次完成有意义的变更后立即追加条目，不等用户要求。遗漏 = 规则违反。
- **记录时机**：代码修改、配置变更、问题修复、架构决策、运行时同步
- **格式**：倒序，每条 2-5 行。写清：改了什么、为什么、当前状态。禁止长篇诊断过程。
- **归档**：超过 30 天的条目移到 `docs/archive/`，WORK_LOG.md 只保留近 30 天。
- **MAINTENANCE_LOG.md** 已废弃，不再使用。

## Skill 管理

详见全局 `CLAUDE.md` 的 Skill 管理规范。
