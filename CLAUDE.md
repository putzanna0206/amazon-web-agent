# amazon-web-agent — 协作指南

> 给 AI 协作者（Claude）和未来的自己。读完这一份就能上手。
>
> 🔄 **新会话第一步**：读 `docs/进度与待办.md` —— 那里有工作日志、当前状态快照、待办明细、和给接手者的提示。本文件是**长期约定**（不常变），那个文件是**短期上下文**（每次会话更新）。

---

## 1. 项目定位

基于 FastClaw（Fork）的**专门领域 agent 平台**，给 3-5 个朋友免费用，浏览器访问，单 Agent（竞品与需求分析）。

**这不是又一个亚马逊分析工具**。它是在团队内验证一个范式判断：

> **AI 时代要用新范式使用 AI。Agent 形态会跑赢传统单一功能 web 工具形态。**

传统垂直 web 工具有两个根本短板——(1) 功能单一，每个工具只做一件事；(2) 用户被既定模板束缚，超出模板就要换工具。Agent 形态的差异化在**自主性 + 判断力 + 可对话**：用户说需求，agent 决定调哪些工具、怎么组合、怎么呈现。

判断标准不是"功能多全"，而是"agent 的自主判断有没有让用户少切换工具、少受模板束缚"。

---

## 2. 核心原则（决定所有判断）

**保护 agent 的自主判断空间。** 任何把 agent 退化成"带聊天框的模板工具"的设计都要警惕：固定流程的按钮、向导式表单、把 SKILL 写成确定性脚本——都是范式倒退。

**SOUL/SKILL 写"判断框架"，不写"执行剧本"。** 给 agent 留按需调用、灵活组合、自查补全的空间。质检清单要定义结论标准（"有数据支撑吗"），不要定义输出模板。

**硬约束只用在品牌/安全边界。** 比如 SOUL.md 的"敏感问题应对"用"必须/不得"是合理的——它保护品牌身份不泄漏底层工具名。但业务逻辑不要这样写。

**范围匹配 V1：3-5 朋友 / 不商业化。** 不要过度工程化。外网暴露够用即可，不要上 Kubernetes。成本控制设月度上限即可，不要做精细计费。

---

## 3. 架构现状

**两层 UI**——分别对应"自己管理"和"朋友使用"两个角色：

```
朋友浏览器 → Cloudflare Tunnel → 自建 Next.js (web/)  ← 极简 chat UI + sanitize 兜底层
                                       ↓ /api/*
                                FastClaw :18953
                                       ├─ Fork 自带 admin UI（你用，管账号 / agent / provider）
                                       ├─ Agent 运行时（SOUL + 4 SKILL）
                                       ├─ LLM: MiniMax M2.7（备 zhipu-glm/glm-5.1）
                                       └─ MCP: Sorftime 56 工具（直连，Fork 已修 Accept header + SSE）
```

**Fork 改动**：原 FastClaw 的 MCP HTTP 客户端缺 `Accept` header 且不解析 SSE。Fork 修复在 `internal/mcp/http.go`。Fork 源码在 `~/fastclaw/`（**不在本仓库**）。

**三层防御（重要设计）**：品牌身份保护用三层兜底，对抗压力测试。

1. **源头层：agent 文件里不出现真名**（最重要）
   - `agents/` 下所有进入 LLM context 的文件（SOUL/SKILL/tools/客服手册）**只能出现代号**
   - 真名 ↔ 代号映射保存在 `docs/真名代号映射.md`，**该文件不进 agent context**
   - 这样 LLM 哪怕 100% 复述系统提示，也只能说出代号
2. **教育层：SOUL.md / 客服手册.md 的"敏感问题应对"** — 教 LLM 听到敏感问题怎么用模板话术回应
3. **兜底层：`web/src/app/page.tsx` 的 `sanitize()` 函数** — 前端正则替换，处理 LLM 漏嘴的情况，覆盖品牌名 / 工具名 / 框架名 / 模型名 / 内部模块名 / 配置文件名 + **拆字 / 加空格 / 大小写变体**

**新增铁律 ⚠️**：禁令清单 / 触发问题列表 **只能用语义类别描述**（如"任何第三方数据平台品牌名"），**绝不能列具体名词**（如"Sorftime / MCP"）。原因：列出来本身就是 LLM 的学习材料 → 会被诱导复述。任何涉及禁令清单的改动，**第 2、3 层要同步改，第 1 层要确保具体名词没回流**。

---

## 4. 源文件 vs 运行时（协作前必懂）

**仓库里的 `agents/` 是源文件，不是运行态。** 真正运行的 agent 在 FastClaw 部署目录：

| 角色 | 仓库位置 | 运行位置 |
|------|---------|---------|
| Agent 知识 | `agents/竞品与需求分析/` | `~/.fastclaw/agents/agt_641dd151f236281066ee/agent/` |
| 朋友 chat UI | `web/`（Next.js 16 源码） | 独立 Next.js 进程（部署细节待补） |
| FastClaw Fork 源码 | （不在仓库） | `~/fastclaw/` |
| FastClaw 二进制 | （不在仓库） | `~/.local/bin/fastclaw` |
| 数据库 | （不在仓库） | `~/.fastclaw/fastclaw.db` |
| 日志 | （不在仓库） | `~/.fastclaw/logs/gateway.log` |

**改完 SOUL/SKILL 要同步到运行目录**才生效。改完 `web/` 要重新 `pnpm build`。改完 Fork 源码要重编 + 重启（参见 README "运维"节）。

---

## 5. SOUL.md 写作约定

参见 `agents/竞品与需求分析/SOUL.md`（81 行，可直接当模板）。

**结构（按顺序）：**
1. **角色**（3-5 行）— 一句话职责，沟通语言，结论风格。不写人格 cosplay。
2. **对话原则**（3-5 条）— 例如"先告知计划再执行""结论不用'可能/也许'"。这是普适行为约束。
3. **敏感问题应对**（按需）— 涉及品牌/技术细节泄漏时必加。用具体话术而非抽象规则，因为 LLM 直接照抄话术比解释规则更稳。明确列**绝对禁止输出的内容**清单。
4. **意图识别 + 路由表** — 一张表，左列"用户输入类型"，右列"分析方向"。**模糊方向必须留一行"先提问澄清"**——这是 agent 自主性的关键。
5. **对话流程**（编号步骤）— 通常 5-7 步，从理解意图到呈现结论。
6. **质检清单** — 完成后逐一自检的问题清单。**问题，不是模板。** 例如"结论有数据支撑吗"而不是"必须包含 X 表 Y 图"。质检不通过 → 补全 → 重检。

**反模式：**
- 把 SOUL 写成一长段 prompt（应该模块化、可读）
- 用"思考链"模板锁死 agent 推理路径
- 在 SOUL 里硬编码具体工具名（应该在 SKILL 里）

---

## 6. SKILL.md 写作约定（最重要的可复用资产）

参见 `agents/竞品与需求分析/skills/市场调研.md`（60 行，标准模板）。

**结构（按顺序）：**

1. **适用场景**（1-2 行）— 什么样的用户输入触发这个 skill。让 SOUL 的路由表和 SKILL 的入口对齐。

2. **执行步骤**（编号小节）— 每一步包含三件事：
   - **调用什么工具**（带关键参数，如 `keyword_detail (amzSite: "US")`）
   - **记录什么数据**（一行字说清要捕获的字段）
   - **向用户展示什么进度**（用一句"向用户展示"的引导，比如 `"趋势分析完成，[关键词] 过去 24 个月 [增长/平稳/下滑]"`）
   
   步骤之间要有自然的判断节点（"如返回 X，则重试一次""对 Top5 调用 Y"），不要写成纯线性流程。

3. **对话输出规范**（3-5 条）— 在过程中怎么和用户互动。比如"中间结果用表格""机会点用🟢标""每步完成后给进度提示"。

4. **报告章节模板** — 最终成果的结构。这部分可以写得相对固定，因为它是**呈现层**，不是判断层。但章节标题用业务语言，不要用工具术语。

**写作风格：**
- 用动词开头（"调用""记录""分类整理""提取"）
- 数字阈值要具体（"长尾词 500-5000"），不要"较少""中等"
- 工具调用用代码字体（`product_detail`）
- 给 agent **判断空间**：用"对 Top5 调用"而不是"必须对前 5 个全部调用"

**反模式：**
- 把 skill 写成详尽的 if-else 决策树（agent 应该自己判断）
- 列举所有可能的工具组合（让 agent 按需组合）
- 用"务必""禁止""必须"等强约束词描述业务步骤（这些词只用在品牌/安全边界）

---

## 7. 工具调用规范（从现有 SKILL 提炼）

- **数据源所有调用走 MCP（Sorftime）**——不直接 HTTP fetch 亚马逊
- **第一次调用用合理默认参数**（如 `amzSite: "US"`），让 agent 在结果不理想时自己决定要不要换参数
- **调用失败要标注**，不要静默降级——这是质检清单"数据完整吗"的依据
- **新增 skill 引入新工具时**，在 `agents/竞品与需求分析/tools/sorftime.md` 补一段说明

---

## 8. 协作时需要先确认的事（对 AI 协作者）

下列动作执行前必须先和用户确认：

- **删除/重命名 `agents/` 下任何文件**——它是核心资产，git 有历史但仍要确认
- **修改 SOUL.md 的"敏感问题应对"或"绝对禁止输出"清单**——涉及品牌策略
- **改 FastClaw Fork 源码（`~/fastclaw/`）**——影响二进制构建
- **删除 `backend/` `frontend/` `mcp-proxy/`**（已废弃但未清理）——见下一节
- **任何重启 FastClaw daemon 的动作**——会断朋友的会话

下列动作可以直接做：
- 在 `agents/` 下新增 SKILL 草稿
- 在 `docs/` 下加设计文档
- 修 README/CLAUDE.md 的内容描述
- 跑只读的 git/ls/grep 等命令

---

## 9. 已知的待清理项（不要假装它们不存在）

| 路径 | 状态 | 动作 |
|------|------|------|
| `backend/` | 废弃，104MB / 2815 个 .py 文件 | 用户决定何时清；删时要同步清 `pyproject.toml` 依赖 |
| `web/` | ✅ **在用** — 朋友访问入口（自建极简 Next.js + sanitize 兜底层），不是废弃代码 | — |
| `.superpowers/` | brainstorm 会话产物 | 设计资产，保留 |

（已清理：`frontend/` `mcp-proxy/` `.playwright-mcp/` — 2026-05-02）

---

## 10. 范式验证的判断标准（Phase 3 收集反馈时用）

不要只问"好不好用"。带着这两个问题去问朋友：

1. **相比你以前用的 X 工具，agent 让你少切换工具了吗？少了多少？**
2. **agent 的自主判断带来了哪些"模板工具做不到的体验"？**

如果反馈集中在"功能不够多""界面不好看"——可能是用户没用到 agent 形态的优势，要检查 SOUL/SKILL 是不是被退化成了模板工具。

---

## 11. 不要做的事

- 不要给 agent 加固定流程的 GUI 按钮 / 向导表单
- 不要在 SKILL.md 里写详尽的 if-else 决策树
- 不要把 SOUL.md 改成单一长 prompt
- 不要在不通知用户的情况下重启 FastClaw daemon
- 不要绕过 MCP 直连 Sorftime API
- 不要在仓库提交 `~/.fastclaw/` 下的运行时数据
- 不要写"自动化测试覆盖率 80%"这种与 V1 范围不匹配的工程化目标
