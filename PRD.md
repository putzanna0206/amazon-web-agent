# Amazon Web Agent — PRD v0.2

> 状态：框架已锁定
> 创建：2026-04-30
> 类型：个人/小圈子工具，非商业化

---

## 一句话定义

云端**多 Agent 框架**，每个 Agent 专一负责一个亚马逊运营任务。给 3-5 个朋友免费使用，浏览器访问，零安装。

V1 只做 1 个 Agent：**竞品与需求分析**。

---

## 1. 已锁定（不会再变）

| 项 | 值 |
|---|---|
| 项目名 | `amazon-web-agent` |
| 用户 | 3-5 个朋友，免费 |
| 商业化 | V1 不做 |
| 架构 | **多 Agent 框架**（每个 Agent 自带知识 + 工具） |
| UI | **Agent 卡片墙** + 进入后聊天 |
| V1 Agent 数量 | **1 个**：竞品与需求分析 |
| 时间盒 | 6 周（Phase 1/2/3 = 3/2/1 周） |

---

## 2. 架构

```
浏览器（朋友）
    ↓ HTTPS
[反向代理 / Cloudflare Tunnel]
    ↓
┌─────────────────────────────────┐
│  Web 前端                        │
│  ├─ Agent 卡片墙（首页）          │
│  └─ 聊天界面（进入 Agent 后）     │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│  Agent 后端                      │
│  ├─ Agent 注册/路由              │
│  ├─ 模型适配层（可插拔）          │
│  └─ FastClaw 运行时              │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│  各 Agent 配置（独立目录）        │
│  └─ agents/竞品分析/             │
│      ├─ SOUL.md（领域知识）       │
│      ├─ tools/（专用工具）        │
│      └─ examples/（开场示例）     │
└─────────────────────────────────┘
```

### 文件结构

```
amazon-web-agent/
├── PRD.md
├── README.md
├── frontend/             # Web UI
├── backend/              # FastClaw + Agent 路由
└── agents/
    └── 竞品分析/
        ├── SOUL.md
        ├── tools/
        └── examples/
```

---

## 3. Phase 1：搭建框架（第 1-3 周）

**目标**：平台骨架跑通，零真实 Agent 内容。

### 交付物
- Web 前端：Agent 卡片墙 + 聊天界面
- 后端：FastClaw 集成 + Agent 注册/路由机制
- 1 个占位 Agent（echo bot），证明端到端通路
- 云端部署 URL，能从外部访问
- 基础认证（朋友能进、陌生人进不来）

### 成功标准
> 你打开浏览器 → 看到 Agent 卡片 → 点击进入 → 聊天发消息 → 收到回复

### 这阶段不决定
- 真实 Agent 内容
- SOUL.md 怎么写
- 用什么模型
- 用什么工具

---

## 4. Phase 2：注入第一个 Agent（第 4-5 周）

**目标**：竞品与需求分析 Agent 跑通。

### 交付物
- `agents/竞品分析/SOUL.md`（领域知识）
- 工具集（Sorftime / 评论抓取等，具体届时定）
- 报告输出格式（建议：价格结构 / 评论挖掘 / 流量结构 / 利润测算 / 机会点结论）
- 模型选择（届时对比 Claude/GPT/qwen）

### 成功标准
> 输入 5 个真实 ASIN，5 分钟内拿到一份你自己满意的竞品分析报告

### 这阶段才决定
- 模型选择
- 工具集
- SOUL.md 内容结构
- 报告字段

---

## 5. Phase 3：验证（第 6 周）

**目标**：朋友真正用起来。

### 交付物
- 邀请 3-5 个朋友试用
- 反馈收集（微信群 / 简单问卷）
- Go/No-Go 决策

### 成功标准
- 🟢 强信号：≥3 个朋友每周主动用 ≥1 次
- 🟡 弱信号：朋友只用一次就不再用
- 🔴 失败：你自己都不每周用

### 这阶段才决定
- 朋友具体是谁
- 数据隔离策略
- 是否扩展到第二个 Agent
- 是否考虑商业化

---

## 6. 明确不做（防蔓延）

| 不做项 | 备注 |
|---|---|
| 移动 App | 浏览器够用 |
| 公网开放注册 | 只给朋友 |
| 付费/商业化（V1） | 6 周后再议 |
| 多 Agent（V1） | 框架支持扩展，但 V1 只 1 个 |
| 历史档案 / 长期数据库 | "运行即用即走" |
| 自由聊天主入口 | 进 Agent 才聊天，不做"通用 AI 助手" |
| 团队协作（共享对话、批注） | 朋友各自独立用 |

---

## 7. 风险

| 风险 | 缓解 |
|---|---|
| Phase 1 框架过度设计，3 周做不完 | 死守"1 个 echo Agent + 端到端通路"最小目标 |
| Phase 2 SOUL.md 写不出真东西 | 用你自己真实工作流喂养，不凭空抽象 |
| Phase 3 朋友不用 | Phase 3 前你自己必须每周用，否则不要邀请 |
| API 成本失控 | Phase 2 跑通后立即设月度上限 |
| 场景蔓延（朋友说"再加个 X"） | 6 周内只做 1 个 Agent，硬性拒绝 |

---

## 8. 后续衔接

- Phase 1 启动 → `/plan-eng-7Y` 规划技术方案
- Phase 2 SOUL.md 写之前 → `/user-model-7Y` 对你自己的工作流建模
- Phase 3 复盘 → `/retro-7Y`

---

## 变更日志

- v0.1 (2026-04-30)：初稿，单 Agent 假设
- v0.2 (2026-04-30)：改为多 Agent 框架；引入 3 阶段结构；明确各阶段才决定的事项
