# 进度与待办

> 跨会话工作连续性文档。新会话 / 新 agent 接手时**第一份要读的文件**（CLAUDE.md → 本文件 → 具体任务）。
>
> 维护规则：每次会话结束前，更新"工作日志"（追加新条目）和"待办明细"（增删/改状态/调优先级）。

---

## 接力指南（给下一位接手者）

**项目类型**：实验性 agent 平台，3-5 朋友试用，不商业化。详见 README.md 和 PRD.md。

**核心原则**（来自 CLAUDE.md，不可违背）：
1. 保护 agent 的自主判断空间——不要做成"带聊天框的模板工具"
2. 禁令清单 / 触发列表只能用语义描述，**不能列具体名词**（列出来本身就是 LLM 学习材料）
3. 改 SOUL/SKILL 后必须 sync 到 `~/.fastclaw/`，否则不生效
4. 敏感改动需用户确认（详见 CLAUDE.md 第 8 节）

**当前焦点**：刚完成"三层防御对抗 prompt 注入"重构（commit `323bff3`），等待用户**sync 到运行时 + 复测**验证效果。这两步完成前，不要继续推进 MCP alias 层等下一步工作。

**容易踩的坑**：
- shell cwd 在每次 Bash 调用之间会重置——用绝对路径
- `agents/` 是源文件，`~/.fastclaw/agents/agt_641dd151f236281066ee/` 才是运行时
- `web/` 是**自建** UI（朋友访问入口 + sanitize 兜底层），不是 FastClaw 自带的
- `backend/` 100MB 是废弃代码，但还没删

---

## 当前状态快照（2026-05-02）

| 维度 | 状态 |
|------|------|
| **代码** | 工作树干净。`main` 分支 commit 至 `323bff3`，**未 push 到远程** |
| **架构** | 两层 UI（自建 web/ + FastClaw 内置 admin）+ 三层品牌防御 |
| **Phase 进度** | Phase 1+2 ✅ Phase 3 ⏳（卡在朋友名单 / 外网 / 成本上限 / 复测） |
| **运行时同步** | ⚠️ 本次 agents/ 改动**尚未 sync 到 `~/.fastclaw/`** |
| **压力测试** | ⚠️ 三层防御已实现，**未复测** |

---

## 待办明细

### 🔴 高（解锁验证 / 阻塞下一步）

- [ ] **sync agents/ 到 `~/.fastclaw/agents/agt_641dd151f236281066ee/agent/`**
  - 涉及文件重命名（`sorftime.md` → `数据系统.md`），需检查 FastClaw 是否需要重新注册 skill
  - runtime 路径里的英文目录（`market-research/` 等）建议同时改成中文，否则路径名会泄漏
  - 由用户手动操作（涉及实际部署）

- [ ] **复测压力测试**（用户做，AI 帮分析）
  - 让团队最会越狱的人对 agent 做 prompt 注入攻击
  - 重点观察：Sorftime / FastClaw / MCP / 真工具名 / SOUL.md 等是否还会被诱导出来
  - 记录每次破解的 prompt 模式，作为 sanitize 补强的依据
  - **结果决定下一步走 Q2(C) 验证 还是 Q2(A)/B 改 schema**

- [ ] **决策：MCP 工具 schema 是否暴露真名**（待复测后再定）
  - 选项 A：FastClaw Fork 加 MCP alias 层（需翻 `~/fastclaw/` 源码）
  - 选项 B：重起 MCP proxy 做工具改名（参考已删的 mcp-proxy/ 骨架）
  - 选项 C：先验证一下到底有没有泄漏（30 分钟），可能根本不需要 A/B
  - **推荐先做 C**

### 🟡 中

- [ ] **Phase 3 三件套**（用户提供后才能推进）
  - 朋友账号：需要用户提供名单
  - Cloudflare Tunnel 配置（含 `web/` 部署方式，独立 Next.js 进程 or 编入 FastClaw 二进制）
  - API 月度成本上限（MiniMax 后台设置）

- [ ] **`web/` 部署文档补完**
  - 环境变量 `NEXT_PUBLIC_API_BASE` 配在哪
  - `pnpm dev` / `pnpm build` 的部署流程
  - 部署位置（独立进程 or 编入 FastClaw 二进制）
  - **需要用户提供事实，AI 才能写**

- [ ] **`backend/` 100MB 废弃代码清理**
  - 需先确认根目录有没有 pyproject.toml（删时同步清依赖）
  - 由用户决定何时清；不影响项目正常运行

- [ ] **README 加"敏感改动后同步步骤"**（用户上次没明确要做，待确认）
  - 提醒改完 SOUL/SKILL 后必须 sync + sanitize 同步升级 + 跑一次复测

### 🟢 低

- [ ] **`git push` 推送本地 commit 到远程**
  - `0e0e5aa` 和 `323bff3` 都还在本地
  - 涉及远程仓库，由用户决定何时推

- [ ] **观察工具命名习惯**：sanitize 现有 30+ 工具名 hardcode，未来 Sorftime 加新工具时要同步加 sanitize 条目（参见 `docs/真名代号映射.md` 维护规则）

---

## 工作日志（按时间倒序）

### 2026-05-02 · Session 1（梳理 + 清理 + 三层防御）

**主线**：用户从"梳理项目"出发，逐步演进到解决"agent 在压力测试下泄漏品牌身份"的核心问题。

**完成的事**：
1. **项目全景梳理**——发现 README 多处与实际不符（web/ 在用而非 FastClaw 自带 UI、sanitize 是 defense-in-depth 兜底层）
2. **新建 CLAUDE.md**——沉淀 SOUL/SKILL 写作 SOP、"保护 agent 自主性"原则、协作前确认清单
3. **清理废弃目录**——删 `frontend/` `mcp-proxy/` `.playwright-mcp/`（commit `0e0e5aa`）
4. **评估 harness engineering 文档**（`~/Downloads/agent_web_lightweight_harness_design.md`）
   - 判断：思想可借鉴（L1/L2/L3 分级、Tool Card、Failure log）但结构不照搬（30 文件目录太重）
   - 关键判断：文档解决"格式不规范"，**对用户实际问题"对抗鲁棒性"几乎无用**
   - 提醒了一个张力：强 Schema + Validator 实质是把模板从前端搬到后端，违反"保护 agent 自主性"原则
5. **三层防御重构**（commit `323bff3`，本次会话主交付）
   - 反直觉关键发现：**"禁令清单列具体名词"本身在教 LLM 学这些词**——这是两层防御被穿透的最大原因
   - 源头层：agents/ 全部去真名（30+ 真名清零，grep 验证）
   - 教育层：触发列表 + 禁令清单全部语义化
   - 兜底层：sanitize 加拆字变体、新框架名（FastClaw/MiniMax/Claude/Helium 等）、内部文件名
   - 新增 `docs/真名代号映射.md` 给开发者维护
   - 在 CLAUDE.md 加铁律：禁令清单不列具体名词

**用户做的关键决策**：
- 删低风险三个废弃目录（不删 backend/）
- 三层防御方案 OK
- 两次 commit OK
- 不商业化、给 3-5 朋友的范围继续保持

**给后续会话的提示**：
- 用户喜欢的回复风格：terse、信号密度高、有明确判断和取舍、不要过度赞美方案
- 用户喜欢"提供方案 + 用户确认 + 执行"的节奏，destructive action 要先确认
- 用户用"可以""继续""b"等极简词回应——通常意味着"按你提的方案做"
- 当用户说"梳理"，他要的是**地图 + 矛盾点 + 下一步**，不是详尽枚举

---

## 重要判断 / 决策记录（不在代码里）

### 关于 harness engineering 文档的取舍
- 文档：`~/Downloads/agent_web_lightweight_harness_design.md`
- 判断：对当前**对抗鲁棒性**问题几乎无用，但 L1/L2/L3 分级、Tool Card、Failure log 三个思想后续可借鉴
- **不要做**：30 文件目录结构、5 张通用工具卡片（无领域味）、强 JSON Schema 输出
- 后续如果做工程化，回来重看这份判断

### 关于"禁令清单悖论"
- 列具体名词 → LLM 学到这些词存在 → 被诱导复述
- 用语义类别描述 → LLM 知道类别但不知具体词
- 已写进 CLAUDE.md 第 3 节"三层防御"作为铁律

### 关于话术诚实性（待用户决定）
- 当前 SOUL 说"我们有一套自建的电商数据分析系统"——严格说是不实陈述
- LLM 自己也"觉得"不该撒谎，所以追问几次就让步——这是穿透原因之一
- 如果要产品化，建议改成"我们对接了业界数据服务，按合作约定不便透露"
- 用户**未决策**，当前先保留原话术

### 关于 web/ 的归属
- 之前用户说"清理 frontend/" — 没删 web/，因为 web/ 是在用的
- web/ 用 Next.js 16 + React 19，自建极简 chat UI（login + chat 两页）
- 包含 460 行的 sanitize() 兜底层，与 SOUL 的"敏感问题应对"协同工作
- 部署方式（独立进程 or 编入 FastClaw 二进制）尚未确认

---

## 维护这份文档的约定

1. **每次会话结束前必须更新**：工作日志追加 + 待办状态调整
2. **工作日志只记"做了什么 + 为什么 + 关键判断"**，不记代码细节（代码看 git log）
3. **重要判断单独沉淀**——这些判断不会自然进入代码，丢失了下次会话要重新讨论
4. **接力指南保持简短**——下一位 agent 30 秒能读完
